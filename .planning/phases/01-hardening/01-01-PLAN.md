---
phase: 01-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [requirements.txt, Dockerfile, src/train.py]
autonomous: true
requirements: [ENV-01, CODE-01, CODE-02, CODE-03]
user_setup: []

must_haves:
  truths:
    - "Environment uses Torch 2.10.0 and Transformers 5.5.4"
    - "Collator concatenates pixel_values and image_grid_thw on dim=0"
    - "Collator pads rope_deltas, mm_token_type_ids, and other sequence tensors"
    - "Model forward pass receives 'images' or 'pixel_values' as expected"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned dependency versions"
      contains: "torch==2.10.0"
    - path: "Dockerfile"
      provides: "Base image and build logic"
      contains: "pytorch/pytorch:2.10.0"
    - path: "src/train.py"
      provides: "Corrected collation and training logic"
      contains: "torch.cat"
  key_links:
    - from: "src/train.py"
      to: "GlmOcrForConditionalGeneration"
      via: "Trainer and DataCollator"
      pattern: "ManualMultimodalDataCollator"
---

<objective>
Harden the GLM-OCR training pipeline by aligning the environment with model requirements and correcting the multimodal data collation strategy.

Purpose: Prevent runtime crashes due to version mismatches and incorrect tensor shapes during multimodal training.
Output: Updated requirements, Dockerfile, and training script.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-research/01-RESEARCH.md
@src/train.py
@requirements.txt
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Environment Specifications</name>
  <files>requirements.txt, Dockerfile</files>
  <action>
    - Update `requirements.txt` to pin:
        - `torch==2.10.0`
        - `torchvision==0.25.0`
        - `transformers==5.5.4`
        - `accelerate==1.13.0`
        - `peft==0.14.0`
    - Update `Dockerfile` to use `FROM pytorch/pytorch:2.10.0-cuda12.4-cudnn9-devel` (or closest stable 2.10.0 devel image).
    - Ensure `flash-attn` version is compatible with the new Torch version (likely 2.7.0+ for Torch 2.10).
  </action>
  <verify>
    <automated>grep "torch==2.10.0" requirements.txt && grep "FROM pytorch/pytorch:2.10.0" Dockerfile</automated>
  </verify>
  <done>Environment files updated to RESEARCH.md specifications.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ManualMultimodalDataCollator</name>
  <files>src/train.py</files>
  <action>
    - Modify `ManualMultimodalDataCollator` in `src/train.py`:
        - **Sequence Tensors**: Use `pad_sequence` for `input_ids`, `labels`, `mm_token_type_ids`, `attention_mask`, AND `rope_deltas`.
        - Use appropriate `padding_value`: `-100` for labels, `tokenizer.pad_token_id` for `input_ids`, `0` for others.
        - **Multimodal Tensors**: Use `torch.cat` with `dim=0` for `pixel_values` and `image_grid_thw`.
        - **Keyword Mapping**: Map `pixel_values` to `images` in the batch dictionary to ensure compatibility with `GlmOcrForConditionalGeneration` forward signature.
    - Ensure `preprocess_function` correctly includes `rope_deltas` if provided by the processor, or initialize them if necessary.
  </action>
  <verify>
    <automated>python -c "import torch; from src.train import train; print('Syntax check passed')"</automated>
  </verify>
  <done>Collator correctly implements concatenation for multimodal tensors and padding for sequence tensors including rope_deltas.</done>
</task>

<task type="auto">
  <name>Task 3: Local Validation Run (Smoke Test)</name>
  <files>src/train.py</files>
  <action>
    - Add a mock data check or use the `--smoke_test` flag to verify the collator logic with a small batch of 2 samples.
    - Run the script locally with `python src/train.py --smoke_test True` (mocking SageMaker paths if necessary or ensuring it fails gracefully on missing data after the collator check).
    - Note: We primarily want to see if `ManualMultimodalDataCollator.__call__` executes without shape mismatches.
  </action>
  <verify>
    <automated>python src/train.py --smoke_test True --train_dir ./dummy_path 2>&1 | grep -E "Starting Training|FileNotFoundError"</automated>
  </verify>
  <done>Collator logic verified through execution (even if it fails later due to missing environment/data, the collation must pass).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dataset → Trainer | Untrusted training data loaded from disk/S3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | requirements.txt | mitigate | Pin exact versions and use hashes if possible (future) |
| T-01-02 | Denial of Service | Collator | mitigate | Ensure padding/concatenation doesn't lead to OOM by checking shapes |
</threat_model>

<verification>
Verify that the `ManualMultimodalDataCollator` successfully handles a batch of features with different image counts/resolutions by using concatenation instead of stacking.
</verification>

<success_criteria>
1. `requirements.txt` and `Dockerfile` reflect RESEARCH.md versions.
2. `src/train.py` contains the rewritten `ManualMultimodalDataCollator`.
3. `rope_deltas` and `image_grid_thw` are correctly handled in the batch.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hardening/01-01-SUMMARY.md`
</output>
